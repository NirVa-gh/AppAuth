using UnityEngine;
using System.Collections;

public class GameInitializer : MonoBehaviour
{
    //[SerializeField] private GameObject authManagerPrefab;
    //[SerializeField] private float initDelay = 0.5f;

    //private IEnumerator Start()
    //{
    //    // Ждем завершения стартовых процессов Unity
    //    yield return new WaitForSeconds(initDelay);

    //    // Создаем AuthManager если его нет
    //    if (AuthManager.Instance == null)
    //    {
    //        Instantiate(authManagerPrefab);
    //        yield return new WaitUntil(() => AuthManager.Instance != null);
    //    }

    //    // Теперь можно запускать основной функционал
    //    Debug.Log("Все системы инициализированы");
    //}
}